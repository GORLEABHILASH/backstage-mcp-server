apiVersion: v1
kind: Service
metadata:
  name: {{ include "backstage-mcp-server.fullname" . }}
  labels:
    {{- include "backstage-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "backstage-mcp-server.selectorLabels" . | nindent 4 }}
